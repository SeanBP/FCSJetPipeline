<?xml version="1.0" encoding="utf-8"?>

<!--
    Jet-calibration pipeline: takes a folder of jet_output_*.root
    (JetTrees) files and a JSON lookup table (from the jet_scale
    pipeline) and adds the corrected branches (reco_E_corr,
    reco_x_F_corr, reco_E_uniform_corr, reco_x_F_uniform_corr) to those
    files in place; ApplyCorrections opens each file with TFile "UPDATE"
    and overwrites the tree, it does not produce separate output files.
    References the shared ../shared/JetParameters.h (computeFeynmanX, for
    reco_x_F_corr/reco_x_F_uniform_corr).

    Configurable per submission:
      {{JETTREES_DIR}}   absolute path to a folder of jet_output_*.root
                          files (a trailing slash is added automatically
                          if missing). All files directly in it are
                          candidates for correction; the total is split
                          evenly across {{NPROCESSES}} jobs (contiguous,
                          non-overlapping ranges, so concurrent jobs never
                          touch the same file).
      {{CALIB_JSON}}      absolute path to the calibration JSON lookup
                          table.
      {{NPROCESSES}}      number of parallel jobs to split the files
                          across.
      {{STDOUT_URL}}      where SUMS copies each job's captured stdout.
      {{STDERR_URL}}      where SUMS copies each job's captured stderr.
                          submit_jet_calibration.sh's -k flag controls
                          both (0 = discard to /dev/null, default; 1 =
                          save under {{JETTREES_DIR}}/log/, auto-created;
                          there's no separate output directory for this
                          pipeline, since it corrects files in place).

    To generate and submit, use submit_jet_calibration.sh (see its -h),
    which wraps the sed substitution + star-submit shown below. The
    SandBox <File> paths below are relative to the pipeline root (SUMS
    packages each <File> preserving that relative path, confirmed
    directly from a real job's .package.zip), so star-submit MUST run
    from there, not from a dedicated submit folder.
    submit_jet_calibration.sh sweeps the sched*/*.package/*.csh/*.list/
    *.condor files star-submit generates into ../submit/ (a dedicated
    folder) right after submission instead:

        cd ..   # pipeline root, if not already there
        sed -e 's#{{JETTREES_DIR}}#/path/to/JetTrees#g' \
            -e 's#{{CALIB_JSON}}#/path/to/lookup.json#g' \
            -e 's/{{NPROCESSES}}/10/g' \
            jet_calibration/jet_calibration.xml > jet_calibration/jet_calibration_myrun.xml
        star-submit jet_calibration/jet_calibration_myrun.xml
        mkdir -p submit && mv sched* submit/ 2>/dev/null

    This is destructive/in-place (the original jet_output_*.root files are
    modified directly, there is nothing to stage back), so double check
    {{JETTREES_DIR}} and {{CALIB_JSON}} before submitting; there is no
    undo.
-->

<job nProcesses="{{NPROCESSES}}">

    <shell>singularity exec -e -B /direct -B /star -B /afs -B /gpfs -B /sdcc/lustre02 /cvmfs/star.sdcc.bnl.gov/containers/rhic_sl7.sif</shell>

    <command>

        stardev

        module purge
        module load fastjet-3.3.4
        module load root-5.34.38

        #--------------------------------------------
        # SUMS preserves the jet_calibration/ and shared/ subfolders when
        # packaging the SandBox; the compile below expects its
        # dependencies flat in the job's working directory.
        #--------------------------------------------
        cp jet_calibration/* .
        cp shared/* .

        echo "Compiling ApplyCorrections..."
        g++ -m64 ApplyCorrections.cpp VectorDict.cxx -o ApplyCorrections \
            `root-config --cflags --libs` -std=c++11

        set INDIR = "{{JETTREES_DIR}}"
        if ( "${INDIR}" !~ */ ) then
            set INDIR = "${INDIR}/"
        endif

        #--------------------------------------------
        # Evenly split every .root file in INDIR across NPROCESSES jobs:
        # contiguous, non-overlapping ranges (first "remainder" jobs get
        # one extra file), so this job only ever opens files no other
        # concurrent job also has open.
        #--------------------------------------------
        ls ${INDIR}*.root | sort > all_files.list
        set NTOTAL = `wc -l &lt; all_files.list`
        set NJOBS = {{NPROCESSES}}

        @ base = ${NTOTAL} / ${NJOBS}
        @ rem = ${NTOTAL} % ${NJOBS}

        if ( ${JOBINDEX} &lt; ${rem} ) then
            @ mycount = ${base} + 1
            @ mystart = ${JOBINDEX} * ${mycount}
        else
            @ mycount = ${base}
            @ mystart = ( ${rem} * ( ${base} + 1 ) ) + ( ( ${JOBINDEX} - ${rem} ) * ${base} )
        endif

        @ line_start = ${mystart} + 1
        @ line_end = ${mystart} + ${mycount}

        echo "========================================"
        echo "JOBINDEX    = ${JOBINDEX}"
        echo "Total files = ${NTOTAL}, total jobs = ${NJOBS}"
        echo "This job's files: lines ${line_start}-${line_end} (${mycount} files)"
        echo "========================================"

        sed -n "${line_start},${line_end}p" all_files.list > my_files.list
        cat my_files.list

        if ( ${mycount} == 0 ) then
            echo "No files assigned to this job (nProcesses exceeds file count); nothing to do."
        else
            echo "Running ApplyCorrections..."
            ./ApplyCorrections my_files.list {{CALIB_JSON}}
        endif

        # Temporary, job-local only; never meant to leave scratch.
        rm -f all_files.list my_files.list

        echo "Done."

    </command>

    <SandBox installer="ZIP">
        <Package>
            <File>file:./jet_calibration/ApplyCorrections.cpp</File>
            <File>file:./shared/VectorDict.cxx</File>
            <File>file:./shared/VectorDict.h</File>
            <File>file:./shared/VectorDictClass.h</File>
            <File>file:./shared/VectorDictLinkDef.h</File>
            <File>file:./shared/JetParameters.h</File>
        </Package>
    </SandBox>

    <stdout URL="{{STDOUT_URL}}"/>
    <stderr URL="{{STDERR_URL}}"/>

</job>
